jQuery(function($) {// 你可以在这里继续使用$作为别名...	leo.init();	$('nav#menu-left').mmenu();});var leo = {	//初始化公共方法	init: function() { 		this.navSearch();		this.popupShare();		this.scrollTop();	},	//导航栏搜索特效	navSearch: function() {		//顶部导航栏搜索特效		var searchInput = jQuery("#s");		var searchform = jQuery("#search");		jQuery(".navbar-search").mouseenter(function(){			jQuery(this).addClass("active");		}).mouseleave(function(){			jQuery(this).removeClass("active");		});		searchform.submit(function(){			if(searchInput.val()=='') {				searchInput.focus();				return false;			}else {				return true;			}		});		//移动端导航搜索框js效果		var ts = jQuery(".toggle-search");		var sr = jQuery(".search-remove");			ts.click(function(){    			jQuery(".search-expand").css("display","block");     		});		sr.click(function(){    			jQuery(".search-expand").css("display","none");      		});			},	//文章页弹框分享	popupShare: function() {		document.onclick = function(e) {			var pp = jQuery('.single-post-social-popup');			e = window.event || e; // 兼容IE7			obj = jQuery(e.srcElement || e.target);			// 点击区域位于当前节点			if (obj.attr('id') == 'share-btn' || obj.attr('id') == 'i-share-btn') {				if (pp.hasClass('active')) {						pp.removeClass("active");				} else {					pp.addClass("active");				}			}else{				//不是当前节点				pp.removeClass("active");       			}		};	},	scrollTop: function() {			//  var prevTop = jQuery(document).scrollTop();		//  var navbarTop= jQuery('.top-navbar').outerHeight();		// jQuery(window).scroll(function() {	 //        var currTop = jQuery(document).scrollTop();	        	 //        if (currTop > navbarTop){ jQuery('.top-navbar').addClass('gizle'); } 	 //        else { jQuery('.top-navbar').removeClass('gizle'); }	 //        if (currTop > prevTop){ jQuery('.top-navbar').removeClass('sabit'); } 	 //        else { jQuery('.top-navbar').addClass('sabit'); }			// 	prevTop = jQuery(document).scrollTop();	 //    });	}	}